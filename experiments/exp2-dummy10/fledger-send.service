[Unit]
Description=Fledger node
After=network.target

[Service]
Type=oneshot
ExecStart=/root/fledger --config /root/flnode -vvv -s ws://${FLEDGER_FLSIGNAL_HOST}:8765 --disable-turn-stun --send-chat-msg ${FLEDGER_SEND_MSG} --timeout 10
RemainAfterExit=yes
User=root
EnvironmentFile=/root/env.systemd

[Install]
WantedBy=multi-user.target
