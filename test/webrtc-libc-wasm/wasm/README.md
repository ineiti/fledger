# Test for connection setup - wasm

The test here and the one in [../libc] are used to verify that the `webrtc`
library can communicate with the browser-webrtc implementation.
It runs using `node`, supposing that it will also run with the browser-webrtc.