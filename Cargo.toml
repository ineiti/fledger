[workspace]

members = [
    "cli/signal",
    "cli/fledger",
    "wasm-webrtc",
    "web",
    "flmodules",
    "flutils",
    "flnet",
    "flnode"
]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
